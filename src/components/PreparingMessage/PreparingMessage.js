import './PreparingMessage.sass';
import React from 'react';

/**
 * PreparingMessage
 * @summary 프리페어링 메시지 컴포넌트
 */
const PreparingMessage = () => (
  <p className="preparing-message">현재 콘텐츠를 준비 중입니다.</p>
);

export default PreparingMessage;
